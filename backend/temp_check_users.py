from django.contrib.auth.models import User; from core.models import UserProfile; print('Superuser:'); su = User.objects.filter(is_superuser=True).first(); print('Username:', su.username if su else 'None'); print('All users:'); users = User.objects.all(); for u in users: profile = UserProfile.objects.filter(user=u).first(); print(f'{u.username}: role={profile.role if profile else "No Profile"}')
